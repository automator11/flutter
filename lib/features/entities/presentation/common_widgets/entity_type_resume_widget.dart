import 'package:flutter/material.dart';

class EntityTypeResumeWidget extends StatelessWidget {
  const EntityTypeResumeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(
      fallbackHeight: 70,
    );
  }
}
